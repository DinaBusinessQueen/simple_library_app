//TODO: разобраться, как сделать локальную базу данных

/*

class UserService {
  Future<User?> getCurrentUser() async {
    // Create a new user
    Future<User> createUser({
      required String name,
      String? email,
      String? photoUrl,
    }) async {}

    // Update user info
    Future<User> updateUser({
      required String name,
      String? email,
      String? photoUrl,
    }) async {}

    // Delete user
    Future<void> deleteUser() async {}

    // Save user to SharedPreferences
    Future<void> _saveUser(User user) async {}

    // BOOKMARKS FUNCTIONALITY

    // Get user bookmarks
    Future<List<Bookmark>> getBookmarks() async {}

    // Add bookmark
    Future<Bookmark> addBookmark({
      required String bookId,
      required String title,
      required String author,
      String? isbn,
      String? thumbnailUrl,
      String? description,
    }) async {}

    // Remove bookmark
    Future<void> removeBookmark(String bookId) async {}

    // Check if a book is bookmarked
    Future<bool> isBookmarked(String bookId) async {}

    // Save bookmarks to SharedPreferences
    Future<void> _saveBookmarks() async {}

    // READING PROGRESS FUNCTIONALITY

    // Save reading progress for a book
    Future<void> saveReadingProgress(String bookId, int pageNumber) async {}

    // Get reading progress for a book
    Future<int> getReadingProgress(String bookId) async {}

    // Clear reading progress for all books
    Future<void> clearAllReadingProgress() async {}

    // Get statistics about user reading
    Future<Map<String, dynamic>> getUserStatistics() async {
     
    }
  }
}
 */
